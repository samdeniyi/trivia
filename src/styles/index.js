import GlobalStyles from './globals';
import { colors, fonts } from './variables';
import { devices, size } from './devices';

export { 
    GlobalStyles, 
    colors, 
    fonts, 
    devices, 
    size 
};